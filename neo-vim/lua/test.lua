-- This module is just here to quickly test that the module loading is working.
-- It should always be the first module loaded. If there are issues on load, comment out the other modules and let this load.
-- If this can't load, then there's something wrong with paths.

print("Test module loaded: Hello World")